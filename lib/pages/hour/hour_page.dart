import 'package:flutter/material.dart';

class HourPage extends StatelessWidget {
  const HourPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Hour Car List')),
      body: Container(),
    );
  }
}